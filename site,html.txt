<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Haveli Restaurant | حویلی ریسٹرنٹ — Rooftop Dining Above Badshahi Mosque, Lahore</title>
<meta name="description" content="Haveli Restaurant — Iconic rooftop dining overlooking Badshahi Mosque & Lahore Fort. Traditional Lahori cuisine in the heart of the Walled City. Rated 4.2★ by 19,000+ guests.">
<meta name="keywords" content="Haveli Restaurant, Lahore, Badshahi Mosque, Pakistani Restaurant, Fort Road Food Street, Rooftop Dining, Lahori Cuisine">

<!-- Fonts -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&family=Cinzel:wght@400;500;600;700&family=Amiri:wght@400;700&family=Jost:wght@300;400;500;600&display=swap" rel="stylesheet">

<!-- Favicon -->
<link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🕌</text></svg>">

<style>
/* ============================================
   HAVELI RESTAURANT — PREMIUM STYLESHEET
   Palette: Deep Maroon · Antique Gold · Ivory · Charcoal
   ============================================ */

:root{
  --maroon-deep: #4a0e0e;
  --maroon: #6b1414;
  --maroon-light: #8a1e1e;
  --gold: #c9a227;
  --gold-light: #e3c565;
  --gold-deep: #9a7a1a;
  --ivory: #f5efe1;
  --ivory-warm: #ede4d0;
  --charcoal: #1a1410;
  --charcoal-light: #2a211a;
  --shadow: rgba(0,0,0,0.5);
  --overlay-dark: rgba(20,12,8,0.72);
  --font-display: 'Cinzel', serif;
  --font-body: 'Cormorant Garamond', serif;
  --font-sans: 'Jost', sans-serif;
  --font-urdu: 'Amiri', serif;
}

*{margin:0;padding:0;box-sizing:border-box;}

html{scroll-behavior:smooth;}

body{
  font-family: var(--font-body);
  background: var(--charcoal);
  color: var(--ivory);
  overflow-x: hidden;
  line-height: 1.7;
  font-size: 17px;
}

/* Scrollbar */
::-webkit-scrollbar{width: 10px;}
::-webkit-scrollbar-track{background: var(--charcoal);}
::-webkit-scrollbar-thumb{background: var(--gold-deep); border-radius: 5px;}
::-webkit-scrollbar-thumb:hover{background: var(--gold);}

/* Selection */
::selection{background: var(--gold); color: var(--charcoal);}

img{max-width:100%; display:block;}

a{color: inherit; text-decoration: none;}

/* ============ UTILITIES ============ */
.container{max-width: 1280px; margin: 0 auto; padding: 0 30px;}
.section{padding: 120px 0; position: relative;}
.text-center{text-align: center;}

.gold{color: var(--gold);}
.urdu{font-family: var(--font-urdu); direction: rtl;}

/* Decorative divider */
.divider{
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 18px;
  margin: 22px auto;
  max-width: 320px;
}
.divider::before,
.divider::after{
  content:'';
  flex:1;
  height:1px;
  background: linear-gradient(90deg, transparent, var(--gold), transparent);
}
.divider-diamond{
  width: 10px; height: 10px;
  background: var(--gold);
  transform: rotate(45deg);
  box-shadow: 0 0 12px var(--gold);
  position: relative;
}
.divider-diamond::before,
.divider-diamond::after{
  content:'';
  position: absolute;
  width: 6px; height: 6px;
  background: var(--gold-deep);
  transform: rotate(45deg);
  top: 2px;
}
.divider-diamond::before{left: -20px;}
.divider-diamond::after{right: -20px;}

/* Section headings */
.section-label{
  font-family: var(--font-sans);
  font-size: 12px;
  letter-spacing: 6px;
  text-transform: uppercase;
  color: var(--gold);
  font-weight: 500;
  margin-bottom: 18px;
  display: block;
}

.section-title{
  font-family: var(--font-display);
  font-size: clamp(2rem, 5vw, 3.6rem);
  font-weight: 500;
  line-height: 1.15;
  letter-spacing: 1px;
  color: var(--ivory);
  margin-bottom: 20px;
}

.section-title .em{
  font-style: italic;
  font-family: var(--font-body);
  color: var(--gold-light);
  font-weight: 400;
}

.section-sub{
  font-family: var(--font-body);
  font-style: italic;
  font-size: 1.15rem;
  color: rgba(245,239,225,0.72);
  max-width: 680px;
  margin: 0 auto;
}

/* Buttons */
.btn{
  display: inline-block;
  padding: 16px 42px;
  font-family: var(--font-sans);
  font-size: 13px;
  letter-spacing: 4px;
  text-transform: uppercase;
  font-weight: 500;
  border: 1px solid var(--gold);
  color: var(--gold);
  background: transparent;
  cursor: pointer;
  transition: all 0.5s cubic-bezier(0.16,1,0.3,1);
  position: relative;
  overflow: hidden;
}
.btn::before{
  content:'';
  position: absolute;
  inset: 0;
  background: var(--gold);
  transform: translateY(100%);
  transition: transform 0.5s cubic-bezier(0.16,1,0.3,1);
  z-index: -1;
}
.btn:hover{color: var(--charcoal);}
.btn:hover::before{transform: translateY(0);}

.btn-solid{
  background: var(--gold);
  color: var(--charcoal);
}
.btn-solid::before{background: var(--gold-light);}

/* ============ NAVBAR ============ */
.nav{
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 1000;
  padding: 22px 0;
  transition: all 0.5s ease;
  background: linear-gradient(180deg, rgba(20,12,8,0.85), transparent);
}
.nav.scrolled{
  background: rgba(20,12,8,0.96);
  padding: 14px 0;
  box-shadow: 0 4px 30px rgba(0,0,0,0.5);
  border-bottom: 1px solid rgba(201,162,39,0.18);
  backdrop-filter: blur(12px);
}
.nav-inner{
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.logo{
  display: flex;
  flex-direction: column;
  line-height: 1;
}
.logo-main{
  font-family: var(--font-display);
  font-size: 1.5rem;
  letter-spacing: 4px;
  color: var(--gold);
  font-weight: 600;
}
.logo-sub{
  font-family: var(--font-urdu);
  font-size: 0.95rem;
  color: var(--ivory);
  margin-top: 4px;
  letter-spacing: 1px;
}

.nav-links{
  display: flex;
  gap: 42px;
  list-style: none;
  align-items: center;
}
.nav-links a{
  font-family: var(--font-sans);
  font-size: 12px;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--ivory);
  font-weight: 400;
  position: relative;
  padding: 6px 0;
  transition: color 0.3s;
}
.nav-links a::after{
  content:'';
  position: absolute;
  bottom: 0; left: 50%;
  width: 0; height: 1px;
  background: var(--gold);
  transition: all 0.4s ease;
  transform: translateX(-50%);
}
.nav-links a:hover{color: var(--gold);}
.nav-links a:hover::after{width: 100%;}

.nav-cta{
  font-family: var(--font-sans);
  font-size: 11px;
  letter-spacing: 3px;
  padding: 11px 24px;
  border: 1px solid var(--gold);
  color: var(--gold);
  transition: all 0.4s;
}
.nav-cta:hover{background: var(--gold); color: var(--charcoal);}

.hamburger{
  display: none;
  flex-direction: column;
  gap: 5px;
  cursor: pointer;
  padding: 8px;
  background: none; border: none;
}
.hamburger span{
  width: 28px; height: 2px;
  background: var(--gold);
  transition: all 0.3s;
}

/* Mobile menu */
.mobile-menu{
  position: fixed;
  inset: 0;
  background: var(--maroon-deep);
  z-index: 999;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 30px;
  transform: translateY(-100%);
  transition: transform 0.6s cubic-bezier(0.16,1,0.3,1);
}
.mobile-menu.open{transform: translateY(0);}
.mobile-menu a{
  font-family: var(--font-display);
  font-size: 1.8rem;
  color: var(--ivory);
  letter-spacing: 3px;
  transition: color 0.3s;
}
.mobile-menu a:hover{color: var(--gold);}
.mobile-close{
  position: absolute;
  top: 30px; right: 30px;
  background: none; border: none;
  color: var(--gold);
  font-size: 2rem;
  cursor: pointer;
}

/* ============ HERO ============ */
.hero{
  height: 100vh;
  min-height: 700px;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  overflow: hidden;
}
.hero-bg{
  position: absolute;
  inset: 0;
  background-image: url('https://images.unsplash.com/photo-1587474260584-136574528ed5?w=1920&q=80');
  background-size: cover;
  background-position: center;
  transform: scale(1.08);
  animation: heroZoom 20s ease-in-out infinite alternate;
  filter: brightness(0.55) saturate(0.85);
}
@keyframes heroZoom{
  0%{transform: scale(1.08);}
  100%{transform: scale(1.18);}
}
.hero-overlay{
  position: absolute;
  inset: 0;
  background:
    radial-gradient(ellipse at center, rgba(20,12,8,0.35) 0%, rgba(20,12,8,0.85) 100%),
    linear-gradient(180deg, rgba(74,14,14,0.35) 0%, rgba(20,12,8,0.7) 60%, rgba(20,12,8,0.98) 100%);
}
.hero-content{
  position: relative;
  z-index: 2;
  padding: 0 20px;
  animation: fadeUp 1.4s ease-out;
}
@keyframes fadeUp{
  from{opacity:0; transform: translateY(40px);}
  to{opacity:1; transform: translateY(0);}
}
.hero-ornament{
  width: 60px; height: 60px;
  margin: 0 auto 24px;
  color: var(--gold);
  opacity: 0.9;
}
.hero-tag{
  font-family: var(--font-sans);
  font-size: 12px;
  letter-spacing: 8px;
  text-transform: uppercase;
  color: var(--gold-light);
  margin-bottom: 26px;
}
.hero-title{
  font-family: var(--font-display);
  font-size: clamp(3rem, 10vw, 7.5rem);
  font-weight: 500;
  letter-spacing: 6px;
  line-height: 0.95;
  color: var(--ivory);
  text-shadow: 0 4px 40px rgba(0,0,0,0.7);
  margin-bottom: 16px;
}
.hero-title span{
  display: block;
  background: linear-gradient(180deg, var(--gold-light) 0%, var(--gold) 50%, var(--gold-deep) 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
}
.hero-urdu{
  font-family: var(--font-urdu);
  font-size: clamp(1.5rem, 3.5vw, 2.4rem);
  color: var(--gold-light);
  margin-bottom: 30px;
  letter-spacing: 2px;
}
.hero-location{
  font-family: var(--font-body);
  font-style: italic;
  font-size: clamp(1rem, 2vw, 1.3rem);
  color: rgba(245,239,225,0.82);
  max-width: 620px;
  margin: 0 auto 42px;
  line-height: 1.7;
}
.hero-buttons{
  display: flex;
  gap: 20px;
  justify-content: center;
  flex-wrap: wrap;
}
.scroll-indicator{
  position: absolute;
  bottom: 34px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 2;
  color: var(--gold);
  font-family: var(--font-sans);
  font-size: 10px;
  letter-spacing: 4px;
  text-transform: uppercase;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  animation: bounce 2.4s infinite;
}
@keyframes bounce{
  0%,100%{transform: translateX(-50%) translateY(0);}
  50%{transform: translateX(-50%) translateY(10px);}
}
.scroll-line{
  width: 1px; height: 40px;
  background: linear-gradient(180deg, var(--gold), transparent);
}

/* ============ ABOUT / STORY ============ */
.about{
  background:
    linear-gradient(180deg, var(--charcoal) 0%, var(--charcoal-light) 50%, var(--charcoal) 100%);
  position: relative;
  overflow: hidden;
}
.about::before{
  content:'';
  position: absolute;
  inset: 0;
  background-image:
    radial-gradient(circle at 15% 25%, rgba(201,162,39,0.06) 0%, transparent 45%),
    radial-gradient(circle at 85% 75%, rgba(107,20,20,0.18) 0%, transparent 45%);
  pointer-events: none;
}
.about-grid{
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 80px;
  align-items: center;
  position: relative;
  z-index: 1;
}
.about-images{
  position: relative;
  height: 600px;
}
.about-img{
  position: absolute;
  border: 1px solid rgba(201,162,39,0.35);
  box-shadow: 0 30px 70px rgba(0,0,0,0.6);
  overflow: hidden;
}
.about-img img{
  width: 100%; height: 100%;
  object-fit: cover;
  transition: transform 0.8s ease;
}
.about-img:hover img{transform: scale(1.06);}
.about-img-1{
  width: 68%; height: 70%;
  top: 0; left: 0;
}
.about-img-2{
  width: 55%; height: 55%;
  bottom: 0; right: 0;
  border-color: var(--gold);
}
.about-badge{
  position: absolute;
  top: 58%;
  right: 8%;
  background: var(--maroon);
  border: 1px solid var(--gold);
  padding: 22px 26px;
  text-align: center;
  z-index: 3;
  box-shadow: 0 20px 50px rgba(0,0,0,0.6);
}
.about-badge .num{
  font-family: var(--font-display);
  font-size: 2.4rem;
  color: var(--gold);
  line-height: 1;
  font-weight: 600;
}
.about-badge .lbl{
  font-family: var(--font-sans);
  font-size: 10px;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--ivory);
  margin-top: 8px;
  opacity: 0.85;
}
.about-text p{
  font-size: 1.12rem;
  color: rgba(245,239,225,0.82);
  margin-bottom: 22px;
  line-height: 1.85;
}
.about-text p:first-of-type::first-letter{
  font-family: var(--font-display);
  font-size: 4rem;
  float: left;
  line-height: 0.8;
  padding: 8px 14px 0 0;
  color: var(--gold);
  font-weight: 600;
}
.about-signature{
  font-family: var(--font-body);
  font-style: italic;
  font-size: 1.3rem;
  color: var(--gold-light);
  margin-top: 32px;
  padding-top: 24px;
  border-top: 1px solid rgba(201,162,39,0.25);
}

/* ============ EXPERIENCE / HIGHLIGHTS ============ */
.experience{
  background: var(--charcoal);
  position: relative;
}
.exp-grid{
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 30px;
  margin-top: 70px;
}
.exp-card{
  background: linear-gradient(160deg, rgba(74,14,14,0.4) 0%, rgba(26,20,16,0.9) 100%);
  border: 1px solid rgba(201,162,39,0.2);
  padding: 52px 36px;
  text-align: center;
  position: relative;
  transition: all 0.5s cubic-bezier(0.16,1,0.3,1);
  overflow: hidden;
}
.exp-card::before{
  content:'';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 2px;
  background: linear-gradient(90deg, transparent, var(--gold), transparent);
  transform: scaleX(0);
  transition: transform 0.6s ease;
}
.exp-card:hover{
  transform: translateY(-8px);
  border-color: rgba(201,162,39,0.5);
  box-shadow: 0 30px 70px rgba(0,0,0,0.5);
}
.exp-card:hover::before{transform: scaleX(1);}
.exp-icon{
  width: 70px; height: 70px;
  margin: 0 auto 26px;
  color: var(--gold);
}
.exp-icon svg{width: 100%; height: 100%;}
.exp-card h3{
  font-family: var(--font-display);
  font-size: 1.5rem;
  color: var(--ivory);
  letter-spacing: 2px;
  margin-bottom: 16px;
  font-weight: 500;
}
.exp-card p{
  font-size: 1rem;
  color: rgba(245,239,225,0.72);
  line-height: 1.8;
}

/* ============ MENU ============ */
.menu-section{
  background:
    linear-gradient(180deg, var(--charcoal-light) 0%, var(--charcoal) 100%);
  position: relative;
}
.menu-section::before{
  content:'';
  position: absolute;
  inset: 0;
  background-image: radial-gradient(circle at 50% 0%, rgba(201,162,39,0.08) 0%, transparent 50%);
  pointer-events: none;
}
.menu-tabs{
  display: flex;
  justify-content: center;
  gap: 12px;
  margin: 50px 0 60px;
  flex-wrap: wrap;
}
.menu-tab{
  font-family: var(--font-sans);
  font-size: 12px;
  letter-spacing: 3px;
  text-transform: uppercase;
  padding: 12px 28px;
  background: transparent;
  border: 1px solid rgba(201,162,39,0.3);
  color: rgba(245,239,225,0.7);
  cursor: pointer;
  transition: all 0.4s;
}
.menu-tab:hover{border-color: var(--gold); color: var(--gold);}
.menu-tab.active{
  background: var(--gold);
  border-color: var(--gold);
  color: var(--charcoal);
  font-weight: 500;
}
.menu-grid{
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 30px 60px;
  position: relative;
  z-index: 1;
}
.menu-item{
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  gap: 16px;
  padding: 20px 0;
  border-bottom: 1px dashed rgba(201,162,39,0.2);
  transition: all 0.4s;
}
.menu-item:hover{
  padding-left: 10px;
  border-bottom-color: var(--gold);
}
.menu-item-info{flex: 1;}
.menu-item-name{
  font-family: var(--font-display);
  font-size: 1.25rem;
  color: var(--ivory);
  letter-spacing: 1px;
  margin-bottom: 6px;
  font-weight: 500;
  display: flex;
  align-items: center;
  gap: 10px;
}
.menu-item-desc{
  font-family: var(--font-body);
  font-style: italic;
  font-size: 0.98rem;
  color: rgba(245,239,225,0.6);
  line-height: 1.55;
}
.menu-item-price{
  font-family: var(--font-display);
  font-size: 1.15rem;
  color: var(--gold);
  font-weight: 600;
  white-space: nowrap;
}
.menu-tag{
  font-family: var(--font-sans);
  font-size: 8px;
  letter-spacing: 2px;
  text-transform: uppercase;
  padding: 3px 8px;
  border: 1px solid var(--gold-deep);
  color: var(--gold);
  border-radius: 2px;
}
.menu-cta{
  text-align: center;
  margin-top: 60px;
}

/* ============ GALLERY ============ */
.gallery{
  background: var(--charcoal);
  padding-bottom: 0;
}
.gallery-grid{
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  grid-auto-rows: 240px;
  gap: 12px;
  margin-top: 60px;
}
.gallery-item{
  position: relative;
  overflow: hidden;
  border: 1px solid rgba(201,162,39,0.15);
  cursor: pointer;
}
.gallery-item img{
  width: 100%; height: 100%;
  object-fit: cover;
  transition: transform 0.9s cubic-bezier(0.16,1,0.3,1), filter 0.5s;
  filter: brightness(0.8);
}
.gallery-item:hover img{
  transform: scale(1.08);
  filter: brightness(1);
}
.gallery-item::after{
  content:'';
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, transparent 50%, rgba(20,12,8,0.7) 100%);
  opacity: 0;
  transition: opacity 0.5s;
}
.gallery-item:hover::after{opacity: 1;}
.gallery-item.tall{grid-row: span 2;}
.gallery-item.wide{grid-column: span 2;}

/* ============ REVIEWS ============ */
.reviews{
  background:
    linear-gradient(180deg, var(--charcoal) 0%, var(--maroon-deep) 100%);
  position: relative;
  overflow: hidden;
}
.reviews::before{
  content:'"';
  position: absolute;
  top: 40px; left: 50%;
  transform: translateX(-50%);
  font-family: var(--font-display);
  font-size: 30rem;
  color: rgba(201,162,39,0.05);
  line-height: 1;
  pointer-events: none;
}
.rating-summary{
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 40px;
  margin: 50px 0 70px;
  flex-wrap: wrap;
  position: relative;
  z-index: 1;
}
.rating-big{
  text-align: center;
}
.rating-num{
  font-family: var(--font-display);
  font-size: 5rem;
  color: var(--gold);
  line-height: 1;
  font-weight: 600;
}
.rating-stars{
  color: var(--gold);
  font-size: 1.3rem;
  letter-spacing: 4px;
  margin: 10px 0;
}
.rating-count{
  font-family: var(--font-sans);
  font-size: 12px;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: rgba(245,239,225,0.7);
}
.rating-divider{
  width: 1px; height: 100px;
  background: linear-gradient(180deg, transparent, var(--gold), transparent);
}
.reviews-grid{
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 30px;
  position: relative;
  z-index: 1;
}
.review-card{
  background: rgba(26,20,16,0.7);
  border: 1px solid rgba(201,162,39,0.2);
  padding: 44px 34px;
  position: relative;
  transition: all 0.5s;
  backdrop-filter: blur(8px);
}
.review-card:hover{
  border-color: var(--gold);
  transform: translateY(-6px);
  box-shadow: 0 30px 60px rgba(0,0,0,0.5);
}
.review-quote-mark{
  font-family: var(--font-display);
  font-size: 4rem;
  color: var(--gold);
  line-height: 0.7;
  opacity: 0.5;
  margin-bottom: 16px;
}
.review-text{
  font-family: var(--font-body);
  font-style: italic;
  font-size: 1.08rem;
  color: rgba(245,239,225,0.9);
  line-height: 1.85;
  margin-bottom: 26px;
}
.review-stars{
  color: var(--gold);
  letter-spacing: 3px;
  margin-bottom: 14px;
  font-size: 0.95rem;
}
.review-author{
  font-family: var(--font-sans);
  font-size: 11px;
  letter-spacing: 3px;
  text-transform: uppercase;
  color: var(--gold-light);
}
.review-author span{
  display: block;
  color: rgba(245,239,225,0.5);
  margin-top: 5px;
  font-size: 10px;
  letter-spacing: 2px;
}

/* ============ LOCATION / CONTACT ============ */
.location{
  background: var(--charcoal);
  position: relative;
}
.location-grid{
  display: grid;
  grid-template-columns: 1fr 1.1fr;
  gap: 70px;
  align-items: stretch;
  margin-top: 60px;
}
.location-info{
  display: flex;
  flex-direction: column;
  justify-content: center;
}
.info-block{
  margin-bottom: 34px;
  padding-bottom: 34px;
  border-bottom: 1px solid rgba(201,162,39,0.18);
}
.info-block:last-child{border-bottom: none; margin-bottom: 0; padding-bottom: 0;}
.info-label{
  font-family: var(--font-sans);
  font-size: 11px;
  letter-spacing: 4px;
  text-transform: uppercase;
  color: var(--gold);
  margin-bottom: 12px;
  display: flex;
  align-items: center;
  gap: 10px;
}
.info-label::before{
  content:'';
  width: 20px; height: 1px;
  background: var(--gold);
}
.info-value{
  font-family: var(--font-body);
  font-size: 1.2rem;
  color: var(--ivory);
  line-height: 1.65;
}
.info-value a{
  color: var(--ivory);
  transition: color 0.3s;
}
.info-value a:hover{color: var(--gold);}
.info-value .urdu{
  display: block;
  margin-top: 6px;
  color: rgba(245,239,225,0.7);
  font-size: 1.05rem;
}
.map-wrap{
  position: relative;
  border: 1px solid rgba(201,162,39,0.35);
  overflow: hidden;
  min-height: 480px;
  box-shadow: 0 30px 70px rgba(0,0,0,0.5);
}
.map-wrap iframe{
  width: 100%; height: 100%;
  min-height: 480px;
  border: 0;
  filter: grayscale(0.4) contrast(1.05);
}

/* ============ HOURS STRIP ============ */
.hours-strip{
  background: var(--maroon);
  border-top: 1px solid rgba(201,162,39,0.3);
  border-bottom: 1px solid rgba(201,162,39,0.3);
  padding: 40px 0;
  position: relative;
  overflow: hidden;
}
.hours-strip::before{
  content:'';
  position: absolute;
  inset: 0;
  background-image:
    repeating-linear-gradient(45deg, transparent, transparent 20px, rgba(201,162,39,0.03) 20px, rgba(201,162,39,0.03) 21px);
  pointer-events: none;
}
.hours-inner{
  display: flex;
  justify-content: space-around;
  align-items: center;
  gap: 40px;
  flex-wrap: wrap;
  position: relative;
  z-index: 1;
}
.hours-item{
  text-align: center;
}
.hours-item .lbl{
  font-family: var(--font-sans);
  font-size: 11px;
  letter-spacing: 4px;
  text-transform: uppercase;
  color: var(--gold-light);
  margin-bottom: 8px;
}
.hours-item .val{
  font-family: var(--font-display);
  font-size: 1.3rem;
  color: var(--ivory);
  letter-spacing: 1px;
}

/* ============ CTA ============ */
.cta{
  padding: 140px 0;
  text-align: center;
  position: relative;
  overflow: hidden;
  background: var(--charcoal);
}
.cta-bg{
  position: absolute;
  inset: 0;
  background-image: url('https://images.unsplash.com/photo-1587474260584-136574528ed5?w=1920&q=80');
  background-size: cover;
  background-position: center;
  background-attachment: fixed;
  filter: brightness(0.28) saturate(0.7);
}
.cta-overlay{
  position: absolute;
  inset: 0;
  background: radial-gradient(ellipse at center, rgba(74,14,14,0.5) 0%, rgba(20,12,8,0.9) 100%);
}
.cta-content{position: relative; z-index: 2;}
.cta h2{
  font-family: var(--font-display);
  font-size: clamp(2.2rem, 5.5vw, 4rem);
  color: var(--ivory);
  letter-spacing: 3px;
  margin-bottom: 20px;
  font-weight: 500;
}
.cta h2 .em{
  font-style: italic;
  font-family: var(--font-body);
  color: var(--gold-light);
}
.cta p{
  font-family: var(--font-body);
  font-style: italic;
  font-size: 1.25rem;
  color: rgba(245,239,225,0.8);
  max-width: 620px;
  margin: 0 auto 42px;
}
.cta-buttons{
  display: flex;
  gap: 20px;
  justify-content: center;
  flex-wrap: wrap;
}

/* ============ FOOTER ============ */
.footer{
  background: #0f0b08;
  padding: 80px 0 30px;
  border-top: 1px solid rgba(201,162,39,0.2);
  position: relative;
}
.footer-grid{
  display: grid;
  grid-template-columns: 1.4fr 1fr 1fr 1fr;
  gap: 50px;
  margin-bottom: 60px;
}
.footer-brand .logo-main{font-size: 1.6rem; margin-bottom: 8px;}
.footer-brand .logo-sub{margin-bottom: 22px;}
.footer-brand p{
  font-size: 1rem;
  color: rgba(245,239,225,0.65);
  line-height: 1.8;
  margin-bottom: 24px;
  max-width: 320px;
}
.footer-social{
  display: flex;
  gap: 12px;
}
.footer-social a{
  width: 40px; height: 40px;
  border: 1px solid rgba(201,162,39,0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--gold);
  transition: all 0.4s;
}
.footer-social a:hover{
  background: var(--gold);
  color: var(--charcoal);
  border-color: var(--gold);
}
.footer-social svg{width: 16px; height: 16px;}
.footer-col h4{
  font-family: var(--font-sans);
  font-size: 12px;
  letter-spacing: 4px;
  text-transform: uppercase;
  color: var(--gold);
  margin-bottom: 24px;
  font-weight: 500;
}
.footer-col ul{list-style: none;}
.footer-col ul li{margin-bottom: 14px;}
.footer-col ul a{
  font-family: var(--font-body);
  font-size: 1.02rem;
  color: rgba(245,239,225,0.72);
  transition: all 0.3s;
}
.footer-col ul a:hover{color: var(--gold); padding-left: 6px;}
.footer-col p{
  font-family: var(--font-body);
  font-size: 1.02rem;
  color: rgba(245,239,225,0.72);
  line-height: 1.8;
  margin-bottom: 12px;
}
.footer-bottom{
  border-top: 1px solid rgba(201,162,39,0.15);
  padding-top: 30px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 20px;
}
.footer-bottom p{
  font-family: var(--font-sans);
  font-size: 11px;
  letter-spacing: 2px;
  color: rgba(245,239,225,0.45);
  text-transform: uppercase;
}
.footer-bottom .urdu{
  font-family: var(--font-urdu);
  font-size: 1.05rem;
  color: var(--gold);
  letter-spacing: 1px;
}

/* ============ ANIMATIONS ON SCROLL ============ */
.reveal{
  opacity: 0;
  transform: translateY(40px);
  transition: opacity 1s ease, transform 1s cubic-bezier(0.16,1,0.3,1);
}
.reveal.visible{
  opacity: 1;
  transform: translateY(0);
}

/* ============ RESPONSIVE ============ */
@media (max-width: 1024px){
  .about-grid{grid-template-columns: 1fr; gap: 60px;}
  .about-images{height: 500px; max-width: 600px; margin: 0 auto; width: 100%;}
  .exp-grid{grid-template-columns: 1fr; max-width: 560px; margin: 70px auto 0;}
  .menu-grid{grid-template-columns: 1fr;}
  .reviews-grid{grid-template-columns: 1fr; max-width: 620px; margin: 0 auto;}
  .location-grid{grid-template-columns: 1fr; gap: 50px;}
  .footer-grid{grid-template-columns: 1fr 1fr; gap: 40px;}
  .gallery-grid{grid-template-columns: repeat(2, 1fr);}
}

@media (max-width: 768px){
  .section{padding: 80px 0;}
  .nav-links{display: none;}
  .nav-cta{display: none;}
  .hamburger{display: flex;}
  .hero-buttons{flex-direction: column; align-items: center;}
  .hero-buttons .btn{width: 100%; max-width: 320px; text-align: center;}
  .about-images{height: 420px;}
  .about-badge{padding: 16px 18px;}
  .about-badge .num{font-size: 1.8rem;}
  .menu-item{flex-direction: column; align-items: flex-start; gap: 8px;}
  .menu-item-price{font-size: 1.05rem;}
  .rating-divider{display: none;}
  .rating-summary{gap: 24px;}
  .footer-grid{grid-template-columns: 1fr; gap: 40px;}
  .footer-bottom{flex-direction: column; text-align: center;}
  .hours-inner{flex-direction: column; gap: 26px;}
  .cta{padding: 100px 0;}
  .gallery-grid{grid-template-columns: 1fr; grid-auto-rows: 260px;}
  .gallery-item.wide, .gallery-item.tall{grid-column: span 1; grid-row: span 1;}
  .section-title{font-size: 2rem;}
}

@media (max-width: 480px){
  .container{padding: 0 20px;}
  .hero-title{letter-spacing: 3px;}
  .divider{max-width: 240px;}
  .review-card{padding: 34px 24px;}
  .exp-card{padding: 40px 26px;}
}

/* Reduced motion */
@media (prefers-reduced-motion: reduce){
  *, *::before, *::after{
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
  .hero-bg{animation: none;}
}
</style>
</head>
<body>

<!-- ============ NAVBAR ============ -->
<nav class="nav" id="nav">
  <div class="container nav-inner">
    <a href="#home" class="logo">
      <span class="logo-main">HAVELI</span>
      <span class="logo-sub urdu">حویلی ریسٹرنٹ</span>
    </a>
    <ul class="nav-links">
      <li><a href="#story">Our Story</a></li>
      <li><a href="#experience">Experience</a></li>
      <li><a href="#menu">Menu</a></li>
      <li><a href="#gallery">Gallery</a></li>
      <li><a href="#visit">Visit Us</a></li>
    </ul>
    <a href="tel:+923008414899" class="nav-cta">Reserve</a>
    <button class="hamburger" id="hamburger" aria-label="Menu">
      <span></span><span></span><span></span>
    </button>
  </div>
</nav>

<!-- ============ MOBILE MENU ============ -->
<div class="mobile-menu" id="mobileMenu">
  <button class="mobile-close" id="mobileClose" aria-label="Close">✕</button>
  <a href="#story">Our Story</a>
  <a href="#experience">Experience</a>
  <a href="#menu">Menu</a>
  <a href="#gallery">Gallery</a>
  <a href="#visit">Visit Us</a>
  <a href="tel:+923008414899" style="color:var(--gold);font-size:1.3rem;">+92 300 8414899</a>
</div>

<!-- ============ HERO ============ -->
<header class="hero" id="home">
  <div class="hero-bg"></div>
  <div class="hero-overlay"></div>
  <div class="hero-content">
    <svg class="hero-ornament" viewBox="0 0 100 100" fill="none" stroke="currentColor" stroke-width="1.5">
      <path d="M50 5 L50 95 M5 50 L95 50 M20 20 L80 80 M80 20 L20 80" opacity="0.3"/>
      <circle cx="50" cy="50" r="30" opacity="0.6"/>
      <circle cx="50" cy="50" r="42" opacity="0.35"/>
      <circle cx="50" cy="50" r="6" fill="currentColor"/>
    </svg>
    <div class="hero-tag">Established in the Walled City of Lahore</div>
    <h1 class="hero-title">
      <span>HAVELI</span>
    </h1>
    <div class="hero-urdu">حویلی ریسٹرنٹ</div>
    <p class="hero-location">
      A rooftop sanctuary above the Badshahi Mosque &amp; Lahore Fort —<br>
      where Mughal grandeur meets the timeless art of Lahori hospitality.
    </p>
    <div class="hero-buttons">
      <a href="tel:+923008414899" class="btn btn-solid">Reserve a Table</a>
      <a href="#menu" class="btn">Explore Menu</a>
    </div>
  </div>
  <div class="scroll-indicator">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</header>

<!-- ============ ABOUT / STORY ============ -->
<section class="section about" id="story">
  <div class="container">
    <div class="about-grid">
      <div class="about-images reveal">
        <div class="about-img about-img-1">
          <img src="https://images.unsplash.com/photo-1587474260584-136574528ed5?w=800&q=80" alt="Badshahi Mosque view from Haveli rooftop">
        </div>
        <div class="about-img about-img-2">
          <img src="https://images.unsplash.com/photo-1601050690597-df0568f70950?w=600&q=80" alt="Traditional Lahori cuisine">
        </div>
        <div class="about-badge">
          <div class="num">4.2</div>
          <div class="lbl">19,149+ Reviews</div>
        </div>
      </div>
      <div class="about-text reveal">
        <span class="section-label">Our Story</span>
        <h2 class="section-title">Above the <span class="em">Walled City,</span><br>Beneath the Stars</h2>
        <div class="divider"><div class="divider-diamond"></div></div>
        <p>
          Perched atop the historic Haveli Khalil Khan on Fort Road Food Street, Haveli Restaurant offers what many call the most spectacular dining view in all of Lahore — a direct, unobstructed panorama of the magnificent Badshahi Mosque and Lahore Fort.
        </p>
        <p>
          This is not merely a restaurant. This is a <em>mehfil</em> — a gathering place where centuries of Lahori tradition, Mughal grandeur, and the timeless art of Pakistani hospitality converge beneath floodlit domes and ancient minarets.
        </p>
        <p>
          As evening falls and the <em>azan</em> echoes across the Walled City, our rooftop becomes a stage for the living history of Androon Lahore — the narrow lanes, the ancient bazaars, the golden era of the Mughals, all unfolding at your feet.
        </p>
        <div class="about-signature">— The Haveli Family</div>
      </div>
    </div>
  </div>
</section>

<!-- ============ EXPERIENCE ============ -->
<section class="section experience" id="experience">
  <div class="container">
    <div class="text-center reveal">
      <span class="section-label">The Haveli Experience</span>
      <h2 class="section-title">Three Reasons to <span class="em">Remember Us</span></h2>
      <div class="divider"><div class="divider-diamond"></div></div>
      <p class="section-sub">Every detail, from the view to the last bite of gulab jamun, is crafted to make your evening unforgettable.</p>
    </div>
    <div class="exp-grid">
      <div class="exp-card reveal">
        <div class="exp-icon">
          <svg viewBox="0 0 64 64" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M8 52 L8 28 L32 12 L56 28 L56 52 Z"/>
            <path d="M24 52 L24 36 L40 36 L40 52"/>
            <circle cx="32" cy="24" r="3"/>
            <path d="M4 52 L60 52"/>
          </svg>
        </div>
        <h3>Rooftop Under the Stars</h3>
        <p>Our iconic terrace places you face-to-face with the Badshahi Mosque's floodlit domes. As the call to prayer echoes across the Walled City, you'll understand why Lahore is called the Heart of Pakistan.</p>
      </div>
      <div class="exp-card reveal">
        <div class="exp-icon">
          <svg viewBox="0 0 64 64" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M32 4 L38 22 L56 22 L42 34 L48 52 L32 40 L16 52 L22 34 L8 22 L26 22 Z"/>
          </svg>
        </div>
        <h3>A Heritage Setting</h3>
        <p>Haveli Restaurant resides within the restored Haveli Khalil Khan — every arch, every carved wooden jharoka, every detail carries the soul of Old Lahore's golden era.</p>
      </div>
      <div class="exp-card reveal">
        <div class="exp-icon">
          <svg viewBox="0 0 64 64" fill="none" stroke="currentColor" stroke-width="1.5">
            <path d="M32 8 C20 8 12 18 12 30 C12 44 22 54 32 56 C42 54 52 44 52 30 C52 18 44 8 32 8 Z"/>
            <path d="M32 20 L32 40 M24 30 L40 30"/>
          </svg>
        </div>
        <h3>Traditional Lahori Cuisine</h3>
        <p>From the charcoal grill to the slow-simmered karahi, every dish is prepared with the <em>andaaz</em> — the instinct and artistry — that defines true Lahori cooking.</p>
      </div>
    </div>
  </div>
</section>

<!-- ============ MENU ============ -->
<section class="section menu-section" id="menu">
  <div class="container">
    <div class="text-center reveal">
      <span class="section-label">From Our Kitchen</span>
      <h2 class="section-title">A Menu <span class="em">Worth the Journey</span></h2>
      <div class="divider"><div class="divider-diamond"></div></div>
      <p class="section-sub">Bold, aromatic flavors that have made Lahore the culinary capital of Pakistan — served with honor.</p>
    </div>

    <div class="menu-tabs reveal">
      <button class="menu-tab active" data-tab="signature">Signature</button>
      <button class="menu-tab" data-tab="karahi">Karahi</button>
      <button class="menu-tab" data-tab="grill">From the Grill</button>
      <button class="menu-tab" data-tab="dessert">Desserts</button>
    </div>

    <div class="menu-grid reveal" id="menuGrid"></div>

    <div class="menu-cta reveal">
      <a href="tel:+923008414899" class="btn">Call to Order · +92 300 8414899</a>
    </div>
  </div>
</section>

<!-- ============ GALLERY ============ -->
<section class="section gallery" id="gallery">
  <div class="container">
    <div class="text-center reveal">
      <span class="section-label">A Glimpse</span>
      <h2 class="section-title">Through Our <span class="em">Windows</span></h2>
      <div class="divider"><div class="divider-diamond"></div></div>
    </div>
  </div>
  <div class="container">
    <div class="gallery-grid reveal">
      <div class="gallery-item wide">
        <img src="https://images.unsplash.com/photo-1587474260584-136574528ed5?w=1200&q=80" alt="Badshahi Mosque at dusk">
      </div>
      <div class="gallery-item tall">
        <img src="https://images.unsplash.com/photo-1601050690597-df0568f70950?w=800&q=80" alt="Traditional curry">
      </div>
      <div class="gallery-item">
        <img src="https://images.unsplash.com/photo-1596797038530-2c107229654b?w=800&q=80" alt="Seekh kebabs">
      </div>
      <div class="gallery-item">
        <img src="https://images.unsplash.com/photo-1631515243349-e0cb75fb8d3a?w=800&q=80" alt="Biryani">
      </div>
      <div class="gallery-item wide">
        <img src="https://images.unsplash.com/photo-1567188040759-fb8a883dc6d8?w=1200&q=80" alt="Rooftop dining">
      </div>
      <div class="gallery-item">
        <img src="https://images.unsplash.com/photo-1565557623262-b51c2513a641?w=800&q=80" alt="Naan bread">
      </div>
      <div class="gallery-item">
        <img src="https://images.unsplash.com/photo-1606491956689-2ea866880c84?w=800&q=80" alt="Karahi">
      </div>
    </div>
  </div>
</section>

<!-- ============ REVIEWS ============ -->
<section class="section reviews" id="reviews">
  <div class="container">
    <div class="text-center reveal">
      <span class="section-label">Guest Voices</span>
      <h2 class="section-title">Loved by <span class="em">Thousands</span></h2>
      <div class="divider"><div class="divider-diamond"></div></div>
    </div>

    <div class="rating-summary reveal">
      <div class="rating-big">
        <div class="rating-num">4.2</div>
        <div class="rating-stars">★★★★☆</div>
        <div class="rating-count">19,149+ Google Reviews</div>
      </div>
      <div class="rating-divider"></div>
      <div class="rating-big">
        <div class="rating-num" style="font-size:3rem;">#1</div>
        <div class="rating-stars" style="font-size:1rem;letter-spacing:2px;">ROOFTOP VIEW IN LAHORE</div>
        <div class="rating-count">Badshahi Mosque · Lahore Fort</div>
      </div>
    </div>

    <div class="reviews-grid">
      <div class="review-card reveal">
        <div class="review-quote-mark">"</div>
        <p class="review-text">The views from Haveli Restaurant are incomparable, and its cuisine is equally fabulous and satisfying. An unforgettable evening above the Walled City.</p>
        <div class="review-stars">★★★★★</div>
        <div class="review-author">Ayesha K.<span>Google Review · Lahore</span></div>
      </div>
      <div class="review-card reveal">
        <div class="review-quote-mark">"</div>
        <p class="review-text">We were seated on the rooftop, enjoying a beautiful city view, including the iconic Badshahi Mosque. Everything was stunning — the food, the ambience, the hospitality.</p>
        <div class="review-stars">★★★★★</div>
        <div class="review-author">Hamza R.<span>Google Review · Islamabad</span></div>
      </div>
      <div class="review-card reveal">
        <div class="review-quote-mark">"</div>
        <p class="review-text">If you want the true taste of Lahore with the best view in the city, this is it. The karahi and seekh kebabs were outstanding. A must-visit for anyone in Lahore.</p>
        <div class="review-stars">★★★★★</div>
        <div class="review-author">Sara M.<span>Google Review · Karachi</span></div>
      </div>
    </div>
  </div>
</section>

<!-- ============ HOURS STRIP ============ -->
<div class="hours-strip">
  <div class="container hours-inner">
    <div class="hours-item">
      <div class="lbl">Open Daily</div>
      <div class="val">Lunch · 12 PM</div>
    </div>
    <div class="hours-item">
      <div class="lbl">Kitchen Closes</div>
      <div class="val">Late Night</div>
    </div>
    <div class="hours-item">
      <div class="lbl">Best Time</div>
      <div class="val">Sunset Hour</div>
    </div>
    <div class="hours-item">
      <div class="lbl">Reservations</div>
      <div class="val">+92 300 8414899</div>
    </div>
  </div>
</div>

<!-- ============ LOCATION ============ -->
<section class="section location" id="visit">
  <div class="container">
    <div class="text-center reveal">
      <span class="section-label">Find Us</span>
      <h2 class="section-title">In the Heart of the <span class="em">Walled City</span></h2>
      <div class="divider"><div class="divider-diamond"></div></div>
    </div>

    <div class="location-grid">
      <div class="location-info reveal">
        <div class="info-block">
          <div class="info-label">Address</div>
          <div class="info-value">
            Haveli Restaurant, 2170 Food St, Fort Rd,<br>
            Shahi Mohallah, Walled City of Lahore,<br>
            Lahore, 54000, Pakistan
            <span class="urdu">مقابل بادشاہی مسجد، فورٹ روڈ، لاہور</span>
          </div>
        </div>
        <div class="info-block">
          <div class="info-label">Reservations</div>
          <div class="info-value">
            <a href="tel:+923008414899">+92 300 8414899</a>
          </div>
        </div>
        <div class="info-block">
          <div class="info-label">Website</div>
          <div class="info-value">
            <a href="https://haveli.com.pk" target="_blank" rel="noopener">haveli.com.pk</a>
          </div>
        </div>
        <div class="info-block">
          <div class="info-label">Services</div>
          <div class="info-value">
            Dine-in · Takeout · Delivery<br>
            Vegetarian Friendly · Vegan Options · Halal
          </div>
        </div>
      </div>

      <div class="map-wrap reveal">
        <iframe
          src="https://www.google.com/maps?q=Haveli+Restaurant+Fort+Road+Badshahi+Mosque+Lahore&output=embed"
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          title="Haveli Restaurant Location">
        </iframe>
      </div>
    </div>
  </div>
</section>

<!-- ============ CTA ============ -->
<section class="cta">
  <div class="cta-bg"></div>
  <div class="cta-overlay"></div>
  <div class="container cta-content reveal">
    <span class="section-label">Reserve Your Table</span>
    <h2>The Rooftop <span class="em">Awaits You</span></h2>
    <p>The rooftop fills quickly, especially at sunset. Call ahead to secure your place above the Walled City.</p>
    <div class="cta-buttons">
      <a href="tel:+923008414899" class="btn btn-solid">Call Now · +92 300 8414899</a>
      <a href="https://wa.me/923008414899" target="_blank" rel="noopener" class="btn">WhatsApp Us</a>
    </div>
  </div>
</section>

<!-- ============ FOOTER ============ -->
<footer class="footer">
  <div class="container">
    <div class="footer-grid">
      <div class="footer-brand">
        <div class="logo-main">HAVELI</div>
        <div class="logo-sub urdu">حویلی ریسٹرنٹ</div>
        <p>Where the grandeur of the Mughals meets the warmth of Lahori hospitality. Rooftop dining above the Badshahi Mosque since the golden era of Fort Road.</p>
        <div class="footer-social">
          <a href="https://facebook.com" target="_blank" rel="noopener" aria-label="Facebook">
            <svg viewBox="0 0 24 24" fill="currentColor"><path d="M22 12c0-5.5-4.5-10-10-10S2 6.5 2 12c0 5 3.7 9.1 8.4 9.9v-7H7.9V12h2.5V9.8c0-2.5 1.5-3.9 3.8-3.9 1.1 0 2.2.2 2.2.2v2.5h-1.3c-1.2 0-1.6.8-1.6 1.6V12h2.8l-.4 2.9h-2.4v7C18.3 21.1 22 17 22 12z"/></svg>
          </a>
          <a href="https://instagram.com" target="_blank" rel="noopener" aria-label="Instagram">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="2" width="20" height="20" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1" fill="currentColor"/></svg>
          </a>
          <a href="https://wa.me/923008414899" target="_blank" rel="noopener" aria-label="WhatsApp">
            <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.5 2 2 6.5 2 12c0 1.8.5 3.5 1.3 5L2 22l5.2-1.4c1.4.8 3.1 1.2 4.8 1.2 5.5 0 10-4.5 10-10S17.5 2 12 2zm5.6 14.2c-.2.7-1.2 1.3-2 1.4-.5.1-1.2.1-1.9-.1-.4-.1-1-.3-1.7-.6-2.9-1.3-4.8-4.2-4.9-4.4-.1-.2-1.2-1.6-1.2-3s.7-2.1 1-2.4c.3-.3.6-.4.8-.4h.6c.2 0 .5-.1.7.5.3.7.9 2.4.9 2.6.1.2.1.4 0 .6-.1.2-.2.4-.3.5-.2.2-.3.4-.5.6-.2.2-.3.4-.1.7.2.3.9 1.4 1.9 2.3 1.3 1.1 2.3 1.5 2.6 1.6.3.1.5.1.7-.1.2-.2.8-.9 1-1.2.2-.3.4-.3.7-.2.3.1 1.9.9 2.2 1 .3.2.5.2.6.4.1.2.1.9-.1 1.6z"/></svg>
          </a>
        </div>
      </div>
      <div class="footer-col">
        <h4>Explore</h4>
        <ul>
          <li><a href="#story">Our Story</a></li>
          <li><a href="#experience">Experience</a></li>
          <li><a href="#menu">Menu</a></li>
          <li><a href="#gallery">Gallery</a></li>
          <li><a href="#reviews">Reviews</a></li>
        </ul>
      </div>
      <div class="footer-col">
        <h4>Visit</h4>
        <ul>
          <li><a href="#visit">Location</a></li>
          <li><a href="tel:+923008414899">Reservations</a></li>
          <li><a href="https://wa.me/923008414899" target="_blank" rel="noopener">WhatsApp</a></li>
          <li><a href="https://haveli.com.pk" target="_blank" rel="noopener">Official Site</a></li>
        </ul>
      </div>
      <div class="footer-col">
        <h4>Contact</h4>
        <p>2170 Food St, Fort Rd,<br>Shahi Mohallah, Walled City,<br>Lahore, 54000, Pakistan</p>
        <p><a href="tel:+923008414899" style="color:var(--gold);">+92 300 8414899</a></p>
      </div>
    </div>
    <div class="footer-bottom">
      <p>© 2025 Haveli Restaurant · All Rights Reserved</p>
      <span class="urdu">حویلی ریسٹرنٹ — لاہور کی روح</span>
    </div>
  </div>
</footer>

<script>
/* ============================================
   HAVELI RESTAURANT — INTERACTIONS
   ============================================ */

// ----- MENU DATA -----
const menuData = {
  signature: [
    { name: "Chicken Charsi Karahi", desc: "Cooked down in its own fat and tomato — the way it's been done for generations", price: "Rs. 1,850", tag: "Signature" },
    { name: "Mutton Sulemani Karahi", desc: "Tender mutton in a rich, spice-laden gravy with whole black pepper", price: "Rs. 2,950", tag: "Chef's Pick" },
    { name: "Lahori Chargha", desc: "Whole chicken marinated in yogurt and spices, roasted to golden perfection", price: "Rs. 1,650", tag: "" },
    { name: "Tawa Chicken", desc: "Sizzling, aromatic, and made for sharing", price: "Rs. 1,450", tag: "" },
    { name: "Chicken Cheese Naan", desc: "Because indulgence is part of the experience", price: "Rs. 550", tag: "" },
    { name: "Special Haveli Platter", desc: "A grand selection of kebabs, karahi, naan & raita — for two", price: "Rs. 3,200", tag: "For Two" },
  ],
  karahi: [
    { name: "Chicken Karahi", desc: "Classic Lahori karahi with fresh tomatoes, ginger & green chilies", price: "Rs. 1,650", tag: "" },
    { name: "Mutton Karahi", desc: "Slow-cooked mutton in a rich, aromatic tomato gravy", price: "Rs. 2,850", tag: "" },
    { name: "Beef Karahi", desc: "Tender beef simmered with traditional Lahori spices", price: "Rs. 2,200", tag: "" },
    { name: "Brain Masala (Maghaz)", desc: "A Lahori delicacy — spiced brain masala with fresh coriander", price: "Rs. 1,950", tag: "Delicacy" },
    { name: "Chicken White Karahi", desc: "Creamy, mild, and rich — for those who prefer less heat", price: "Rs. 1,850", tag: "" },
    { name: "Prawn Karahi", desc: "Fresh prawns in a spicy, tangy karahi masala", price: "Rs. 2,650", tag: "" },
  ],
  grill: [
    { name: "Seekh Kebabs", desc: "Hand-rolled, charcoal-grilled, and impossibly juicy", price: "Rs. 950", tag: "Popular" },
    { name: "Chicken Tikka", desc: "Charcoal-grilled chicken with traditional Lahori marinade", price: "Rs. 850", tag: "" },
    { name: "Malai Boti", desc: "Creamy, tender chicken boti grilled to perfection", price: "Rs. 1,050", tag: "" },
    { name: "Beef Boti", desc: "Marinated beef cubes, charcoal-grilled with aromatic spices", price: "Rs: 1,250", tag: "" },
    { name: "Mixed Grill Platter", desc: "Seekh, tikka, boti & malai boti — a feast for two", price: "Rs. 2,750", tag: "For Two" },
    { name: "Fish Tikka", desc: "Fresh fish marinated in Lahori spices, grilled on charcoal", price: "Rs. 1,450", tag: "" },
  ],
  dessert: [
    { name: "Gulab Jamun", desc: "Warm, syrup-soaked dumplings made in-house", price: "Rs. 450", tag: "" },
    { name: "Kheer", desc: "Traditional rice pudding with cardamom & pistachios", price: "Rs. 400", tag: "" },
    { name: "Firni", desc: "Delicate ground-rice pudding in clay bowls", price: "Rs. 400", tag: "" },
    { name: "Shahi Tukray", desc: "Royal Mughlai bread pudding with saffron & cream", price: "Rs. 550", tag: "Royal" },
    { name: "Rabri Falooda", desc: "Chilled falooda with creamy rabri & nuts", price: "Rs. 650", tag: "" },
    { name: "Kulfi", desc: "Traditional dense ice cream with pistachio & almond", price: "Rs. 350", tag: "" },
  ]
};

// ----- RENDER MENU -----
function renderMenu(tab){
  const grid = document.getElementById('menuGrid');
  const items = menuData[tab] || [];
  grid.innerHTML = items.map(item => `
    <div class="menu-item">
      <div class="menu-item-info">
        <div class="menu-item-name">
          ${item.name}
          ${item.tag ? `<span class="menu-tag">${item.tag}</span>` : ''}
        </div>
        <div class="menu-item-desc">${item.desc}</div>
      </div>
      <div class="menu-item-price">${item.price}</div>
    </div>
  `).join('');
}
renderMenu('signature');

// ----- MENU TABS -----
document.querySelectorAll('.menu-tab').forEach(tab => {
  tab.addEventListener('click', () => {
    document.querySelectorAll('.menu-tab').forEach(t => t.classList.remove('active'));
    tab.classList.add('active');
    renderMenu(tab.dataset.tab);
  });
});

// ----- NAVBAR SCROLL -----
const nav = document.getElementById('nav');
window.addEventListener('scroll', () => {
  if (window.scrollY > 60) nav.classList.add('scrolled');
  else nav.classList.remove('scrolled');
});

// ----- MOBILE MENU -----
const hamburger = document.getElementById('hamburger');
const mobileMenu = document.getElementById('mobileMenu');
const mobileClose = document.getElementById('mobileClose');

hamburger.addEventListener('click', () => mobileMenu.classList.add('open'));
mobileClose.addEventListener('click', () => mobileMenu.classList.remove('open'));
document.querySelectorAll('.mobile-menu a').forEach(link => {
  link.addEventListener('click', () => mobileMenu.classList.remove('open'));
});

// ----- SCROLL REVEAL -----
const reveals = document.querySelectorAll('.reveal');
const revealObserver = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting){
      entry.target.classList.add('visible');
      revealObserver.unobserve(entry.target);
    }
  });
}, { threshold: 0.12, rootMargin: '0px 0px -50px 0px' });

reveals.forEach(el => revealObserver.observe(el));

// ----- SMOOTH SCROLL FOR ANCHORS -----
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
  anchor.addEventListener('click', function(e){
    const target = document.querySelector(this.getAttribute('href'));
    if (target){
      e.preventDefault();
      const offset = 80;
      const top = target.getBoundingClientRect().top + window.pageYOffset - offset;
      window.scrollTo({ top, behavior: 'smooth' });
    }
  });
});

// ----- GALLERY LIGHTBOX (simple) -----
document.querySelectorAll('.gallery-item').forEach(item => {
  item.addEventListener('click', () => {
    const img = item.querySelector('img');
    const lightbox = document.createElement('div');
    lightbox.style.cssText = `
      position: fixed; inset: 0; background: rgba(10,6,4,0.95);
      z-index: 9999; display: flex; align-items: center; justify-content: center;
      padding: 40px; cursor: zoom-out; animation: fadeIn 0.4s ease;
    `;
    const imgEl = document.createElement('img');
    imgEl.src = img.src;
    imgEl.style.cssText = `
      max-width: 100%; max-height: 100%; object-fit: contain;
      border: 1px solid rgba(201,162,39,0.4); box-shadow: 0 30px 80px rgba(0,0,0,0.8);
    `;
    lightbox.appendChild(imgEl);
    lightbox.addEventListener('click', () => lightbox.remove());
    document.body.appendChild(lightbox);
  });
});

// ----- FADE IN KEYFRAME (injected) -----
const style = document.createElement('style');
style.textContent = `@keyframes fadeIn{from{opacity:0}to{opacity:1}}`;
document.head.appendChild(style);

console.log('%cحویلی ریسٹرنٹ', 'color:#c9a227;font-size:28px;font-family:serif;');
console.log('%cHaveli Restaurant — Crafted with honor.', 'color:#f5efe1;font-size:14px;letter-spacing:2px;');
</script>

</body>
</html>